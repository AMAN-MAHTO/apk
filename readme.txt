kivy provides us interface through which we can make app in pyhton that can run through all the plateform

kivymd is the kivy language like css that help to style

naming of kv file is the class that we use in our py file or app is not alow in name


https://kivymd.readthedocs.io/en/latest/

git on working branch not master